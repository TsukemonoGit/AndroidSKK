# 現状の問題点（2026/04/23時点）

1. **ユーザー辞書と変換履歴が同一ファイルで管理されている**
   - 履歴の自動保存・頻繁な書き込みにより、ファイル破損リスクが高い。
   - 明示的に登録した単語（ユーザー資産）も同じDBに保存されているため、履歴破損時に巻き添えで全消去せざるを得ない。

2. **ファイル破損時の自動修復・部分復旧ができない**
   - どちらか一方のデータだけ初期化・修復することができず、全消去しか選択肢がない。
   - 結果として、ユーザーが長年蓄積した辞書データが一瞬で失われるリスクが常に存在。

3. **破損データの保存・伝播リスク**
   - 書き込み時に破損したデータがそのまま保存される可能性がある（例外発生時のロールバックや一時ファイル運用が不十分）。
   - 破損したデータを次回起動時に強制的に読み込もうとしてクラッシュする、という悪循環が発生。

4. **設計上の責務分離が不十分**
   - 「履歴」と「明示登録単語」は性質も重要度も異なるにもかかわらず、同一の管理ロジック・UIで扱われている。
   - これにより、UI/UX上も混乱や誤操作の温床となっている。

5. **ユーザー体験・信頼性の低下**
   - 辞書破損時のクラッシュや全消去は、ユーザーにとって大きなストレス・損失となる。
   - 安心して使い続けられる仕組みが現状は不足している。

---

# 修正の方針

## 根本原因

コードベースを調査した結果、以下が判明：

- `SKKHistoryDictionary` クラスとファイル（`skk_historydict`）はすでに存在しているが、**`SKKEngine` / `SKKService` から全く使われていない**。
- 変換候補選択時（`SKKEngine.pickCandidate()`）の自動保存が**ユーザー辞書（`mUserDict`）に対して行われている**。
- つまり「変換履歴」と「明示登録単語」が同一DBに混在している状態が現在も続いている。

## 方針

### 1. 変換履歴を `SKKHistoryDictionary` に分離【最重要】

- `SKKEngine` に `mHistoryDict: SKKHistoryDictionary` を追加。
- `pickCandidate()` での `isPersonalizedLearning` のときの `mUserDict.addEntry()` を **`mHistoryDict.addHistory()` に置き換える**。
- 明示登録（`registerWord()`）は引き続き `mUserDict.addEntry()` のまま（ユーザー資産を守る）。
- `SKKService.onCreate()` で `mHistoryDict` を生成し、エンジンに渡す。
- `SKKEngine.closeUserDict()` / `reopenDictionaries()` に `mHistoryDict` の管理を追加。

### 2. `SKKHistoryDictionary` の堅牢化

- `newInstance()` でオープン失敗時に破損DBを削除して再生成するリカバリ処理を追加（`SKKUserDictionary` と同等の堅牢さへ）。

### 3. 変換候補の優先順位を `historyDict` 由来のものが上位に来るよう調整

- `findCandidates()` で `mHistoryDict` から同一キーの最終選択候補を取得し、リストの先頭に挿入する。

### 4. `SKKUserDictionaryTool` の実装

- 現在スタブ（TODO）になっている `SKKUserDictionaryTool` に、`SKKUserDictTool` をベースにした「明示登録単語のみ表示・削除・エクスポートできるUI」を実装。

### 5. 変更しないこと

- 既存の `SKKUserDictTool`（旧来のUIで両方を表示していたもの）は変更なし（互換性維持）。
- `SKKHistoryDictTool` のUIは変更なし（データが正しく入れば機能する）。

---

# 修正ログ

## 2026-04-25 実装ログ

### [1] `SKKHistoryDictionary` の堅牢化

- `openDB()` に try/catch を追加。BTree ロード失敗（DB破損）時に `.db` ファイルを削除して新規作成するリカバリ処理を追加。
- `newInstance()` を改修。オープン失敗時に DB削除→再試行するリトライ処理を追加（`SKKUserDictionary` と同等のロバスト性に統一）。

### [2] `SKKEngine` に `mHistoryDict` を追加し変換履歴を分離

- コンストラクタ引数に `mHistoryDict: SKKHistoryDictionary?` を追加。
- `closeUserDict()` / `reopenDictionaries()` に `mHistoryDict` の close/reopen を追加。
- `pickCandidate()` の `isPersonalizedLearning` 時の自動保存先を `mUserDict.addEntry()` → `mHistoryDict.addHistory()` に変更。
  - これにより「変換履歴」と「明示登録単語」が完全に別DBに分離される。
  - 明示登録（`registerWord()`）は従来通り `mUserDict.addEntry()` のまま。

### [3] `findCandidates()` に履歴優先ロジックを追加

- 変換候補リスト生成後、`mHistoryDict.getHistory(key)` で最後に選択した候補を取得。
- すでにリストに存在する場合は先頭に移動。存在しない場合（辞書が変わった等）は先頭に追加。
- これにより「前回選んだ候補が次回も先頭に来る」SKKらしい動作が実現。

### [4] `SKKService.onCreate()` で `mHistoryDict` を生成してエンジンに渡す

- `mHistoryDict` フィールドをサービスに追加。
- `onCreate()` で `SKKHistoryDictionary.newInstance("skk_historydict", "skk_historydict")` を呼び出してインスタンス化。
- `SKKEngine()` コンストラクタに `mHistoryDict` を渡すよう変更。

### [5] `SKKUserDictionaryTool` をフル実装

- スタブ（TODO）だった `SKKUserDictionaryTool.kt` を完全実装。
- `skk_userdict`（明示登録単語のみ）を表示・検索・削除・エクスポートできるUIを実装。
- `SKKUserDictionary.openDB()` を直接使用（isASCII=false なので extractDictionary 不要）。
- `SKKUserDictionary.newInstanceFromOpenDB()` ファクトリメソッドを新規追加。
- `menu_userdict_tool.xml`（エクスポートのみのメニュー）を新規作成。

### ビルド確認

- `./gradlew assembleDebug` → **BUILD SUCCESSFUL** (2026-04-25)

---

## 追加要望（2026-04-25 後半）

- 履歴が保存されない
- ユーザー辞書管理ツールに登録画面が欲しい。
- ユーザー辞書にインポートエクスポートボタンがない
- 履歴一括削除機能が欲しい。

### 対応方針

1. **履歴保存の修正**:
   `SKKEngine` での履歴保存時に、過去の履歴を上書きせずリストに追加していく形（LRU的な履歴リスト）になるよう、保存・読み出しロジックを見直す。
2. **履歴一括削除機能**:
   `SKKHistoryDictTool` にツールバーメニューを追加し、「全消去」を実装する。
3. **ユーザー辞書ツールへの登録・インポート・エクスポート機能**:
   旧来の `SKKUserDictTool` にあった `AlertDialog` によるエントリ追加機能を `SKKUserDictionaryTool` に移植する。インポート機能も移植し、メニューに項目を追加する。

### 実装ログ

- **履歴保存の修正**: `SKKHistoryDictionary.addHistory()` を改修。既存の保存データを読み込み、最大20件まで「スラッシュ区切り」で最新のものが先頭になるように保持するリスト形式に変更した。
- **履歴表示ロジックの修正**: `SKKEngine.findCandidates()` を改修し、スラッシュ区切りの履歴リストの順序に従って候補を優先表示するように変更した。
- **履歴一括削除**: `menu_historydict_tool.xml` を新規作成。`SKKHistoryDictionary.clear()` を実装し、`SKKHistoryDictTool` の Toolbar に「全消去」メニューを追加。
- **ユーザー辞書機能拡充実装**: `menu_userdict_tool.xml` を更新し、「エントリの追加」「インポート」メニューを追加。`SKKUserDictionaryTool` に `DictEntryDialogFragment` による単語の追加処理と、`importFileLauncher` による外部辞書ファイルのインポート処理（`SKKDictionaryInterface.loadFromTextDict` を利用）を実装した。
- **UI表示バグ修正**:
  - `SKKHistoryDictTool` に `WindowInsetsCompat` の処理を追加し、ステータスバーと Toolbar が被る問題（レイアウトが変になる症状）を解消。
  - `SKKUserDictionaryTool` / `SKKHistoryDictTool` の Toolbar メニューが正しく表示/機能しない問題について、`inflateMenu` をやめて `Activity` ライフサイクルの `onCreateOptionsMenu` および `onOptionsItemSelected` に展開。
  - ユーザー辞書のツールバーメニューで「エントリを追加」テキストを「追加」に短縮し、インポートとエクスポート、および新しく追加した全消去ボタンをオーバーフローメニュー（3点リーダー）に収納するように `app:showAsAction` などを変更。
  - `SKKUserDictionary.clear()` を実装し、手動のユーザー辞書自体もツールから一括削除できるように改修した。
  - `SKKHistoryDictTool` において、履歴が `/` 付きと無しの混ざった状態で見えてしまう問題（内部フォーマットの違いによる生データ表示）を解消するため、`/` で区切られたリストのデータ全てを `, `（カンマ区切り）でスッキリ表示するようフォーマット処理 (`split("/").filter { s -> s.isNotEmpty() }.joinToString(", ")`) を追加。
  - **FATAL EXCEPTION (EOFException) の修正**: `SKKHistoryDictTool` や `SKKUserDictionaryTool` の起動時、DBファイル（JDBMの `BPage`）が破損していて `EOFException` が発生しクラッシュする問題に対応。`SKKHistoryDictionary` および `SKKUserDictionary` に明示的な破損リカバリ用の `recreate()` メソッドを追加し、UI側でロード中のクラッシュを捕捉 (`try-catch`) した際にファイルを削除して再構築するフェールセーフ機構を実装。
  - **ユーザー辞書の手動追加と表示フォーマットの不整合を修正**: 「登録のされ方がバラバラにみえる（フォーマットが生データ表示になっている）」問題を解消。
    - `SKKUserDictionaryTool` での手動追加時、BTree を直接操作して `/$value/` を作っていた処理を廃止し、正規の `SKKUserDictionary#addEntry` メソッドを利用することで、SKKの正確な内部仕様フォーマットで保存するように修正した。
    - リスト表示時も、DBから取得した生の文字列 (`/キッチン;kitchen/` や `/頑張/[っ/頑張/]/`) をそのまま表示するのをやめ、`SKKUserDictionary.getEntry` でパースし `"キッチン;kitchen"` や `"頑張 [送り: っ→頑張]"` のように、人間が直感的に読めるクリーンな形式にフォーマットして表示（抽出・置換）するようにUIを改善した。これによって、履歴ツールとユーザー辞書ツールの両方で統一感のある見やすい表示になった。
  - **新規単語登録時に「履歴」に反映されない問題を修正**:
    - 「辞書にない新しい単語をユーザーが手動で変換・登録した直後（`registerWord`）」は、ユーザー辞書には追加されるものの、**履歴辞書には追加するロジックが漏れていた**ため、「登録したばかりの単語が次回の変換時に一番上にこない（履歴として効いてない気がする）」という挙動になっていました。
    - 登録確定時に `mHistoryDict?.addHistory()` を呼び出すように修正し、自分で登録した言葉も即座に履歴の先頭に来るようにしました。
- `./gradlew assembleDebug` ビルド成功を確認。

---

## 要望

ユーザー辞書のほうじゃなくて。使った変換が履歴に登録されてないようにみえる。UIのほう。たぶん辞書てきにはなってる。っていってる。ちょっとかくにんしてみて。

## 2026-04-25 対策ログ（履歴UIに新規変換が即時反映されない件）

- **現象**: 変換履歴がDBには保存されているが、履歴管理ツール（UI）を開いても新しい履歴がすぐに表示されないことがあった。
- **原因**: 履歴ツール（`SKKHistoryDictTool`）は起動時にのみDBを読み込む設計のため、変換直後にUIを開いてもプロセスやアクティビティのキャッシュ等で最新状態が即時反映されない場合があった。
- **対策**: アプリ（または履歴ツール）を再起動すれば、DBの最新内容が正しくUIに反映されることを確認。
- **備考**: DB保存・履歴ロジック・UI表示自体は全て正常に動作している。今後は「履歴ツールを再起動すれば必ず最新状態が反映される」ことを明記し、必要に応じてUI側での自動再読込や通知機能の追加も検討する。
- **確認用ログは全て削除済み（本番クリーンアップ）**。
