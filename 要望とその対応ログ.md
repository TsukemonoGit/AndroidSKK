- 絵文字/記号の選択画面について、Gboardのような選択画面になってほしい
  - 絵文字専用の画面。文字入力画面をおおうようにIME全体表示
  - 最新の絵文字対応、検索欄あり - 記号タブもほしい。GIFはいらない。

- ユーザー辞書の登録を設定画面から出来るようにしたい
  - 変換履歴とは別で管理してほしい
  - 入力欄からの変換登録に、₍ ･ᴗ･ ₎等登録できない文字列が存在している(これについては、設定画面での登録が可能なら、入力画面では登録できなくても許容)

- ほかに改善できそうなポイントはあるかなぁ

---

# 2026-04-04

## 実装した変更

### 1. 絵文字/記号選択画面（Gboard風）

- **`androidx.emoji2:emoji2-emojipicker`** ライブラリを追加し、Android標準の絵文字ピッカーを使用
- EmojiPickerKeyboardView.kt — 絵文字/記号タブ切替、検索バー、キーボード復帰ボタン付きのフルスクリーンビュー
- SymbolData.kt — 「記号」タブ用のカテゴリ別記号データ（顔文字、矢印、星/記号、数学、括弧、通貨、単位、罫線/装飾）
- view_emoji_picker.xml — レイアウト
- **FlickJPKeyboardView** と **GodanKeyboardView** の絵文字キーを新ピッカーに接続
- **SKKService** に `showEmojiPicker()` / `hideEmojiPicker()` メソッドを追加

### 2. ユーザー辞書の設定画面からの登録

- DictEntryDialogFragment.kt — 読みと単語の2フィールド入力ダイアログ（`₍ ･ᴗ･ ₎` 等の特殊文字も登録可能）
- **SKKUserDictTool** のメニューに「エントリを追加」を追加
- 変換履歴とは独立した直接登録機能

### 3. その他の改善

- **SKKService.onDestroy()** で `mHandler.removeCallbacksAndMessages(null)` を追加（メモリリーク防止）
- 入力終了時に絵文字ピッカーを適切に閉じる処理

---

# 2026-04-04

- 絵文字を開こうとすると(左下のボタンを左にスワイプすると)アプリが落ちる。

# 修正しました。

原因: showEmojiPicker()で使われるContextにTheme.AppCompat系のテーマが設定されていないため、com.google.android.material.tabs.TabLayoutがinflate時にIllegalArgumentExceptionを投げてクラッシュしていました。

修正内容: SKKService.kt:1378 で、createNightModeContextが返す素のContextをContextThemeWrapperでTheme.SKK（Theme.AppCompat.DayNight.Dialogの子テーマ）でラップするようにしました。これによりTabLayoutが必要とするAppCompatテーマが提供されます。
